import React from "react";

const Button = ({ children }) => (
  <h1 className="px-6 py-4 mx-4 my-3 text-center border rounded cursor-pointer  bg-zinc-300">
    {children}
  </h1>
);

export default Button;
